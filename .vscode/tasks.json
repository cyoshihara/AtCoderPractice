{
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-std=gnu++1y",
                "-g",
                "-O2",
                "${fileDirname}\\${fileBasename}",
                "-o",
                // "${workspaceFolder}\\bin\\${fileBasenameNoExtension}.exe"
                "${workspaceFolder}\\bin\\a.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
}